import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prueba1-CHAT',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
